# ตั้งเป็น 'production' หากไม่ต้องการให้ Log ออกทาง Console รกๆ
# (ERROR ยังลง console เสมอทุก env)
PORT=3000
NODE_ENV=development
VAULT_URL="http://192.168.55.25:8200"
VAULT_USER=""
VAULT_PWD=""
APP_ID=0

# บังคับตาม Zod schema — ถ้าไม่ตั้ง (และ Vault ไม่ได้ให้มา) จะ throw ตั้งแต่ boot
TNS_PATH=
ORACLE_CLIENT_PATH=
